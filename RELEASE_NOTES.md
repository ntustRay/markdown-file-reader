# Ray Markdown Reader — Release Notes

## 1.1.0

- Create Markdown files on your phone: choose **New .md**, select a filename and location, then start writing.
- Keep the current draft if creation is cancelled or fails.
- Preserve edits made while a save is pending and prevent overlapping file operations.
- Reset unsaved-change dialogs so Escape never repeats a previous Discard choice.
- Open local filenames containing literal percent signs correctly.
- Remove unsupported Android TV launcher declarations from the phone release.

Google Play What's new:

```text
<en-US>
Create .md files on your phone with New .md. Choose a name and location, then start writing. Improved protection for unsaved edits and reliability when saving, cancelling, or switching files.
</en-US>
<zh-TW>
新增「新增 .md」功能：在手機選擇檔名與儲存位置後即可開始編寫。改善未儲存內容保護，以及儲存、取消與切換檔案的穩定性。
</zh-TW>
```

## 1.0.0

First Android release candidate.

- Open one local `.md`, `.markdown`, or `.txt` file
- Preview Markdown or read unformatted plain text
- Switch between full-screen Preview and Edit
- Save changes back to the selected file
- Confirm Save, Discard, or Cancel before leaving changed work
- Preserve UTF-8 BOM and LF/CRLF line endings
- Support light/dark themes, English, and Traditional Chinese
- Open only HTTP(S) links in the external browser
- No accounts, ads, tracking, cloud sync, or broad storage access

Google Play What's new:

`First release: open, preview, edit, and save local Markdown and text files.`
