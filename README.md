# Markdown File Reader

一個使用 Tauri 構建的跨平台 Markdown 閱讀器應用程式。

## 功能特性

- 開啟並渲染 Markdown 檔案
- 支援 GitHub Flavored Markdown (GFM)
- 明暗主題切換功能
- 美觀的閱讀介面
- 跨平台支援 (Windows, macOS, Linux)
- 小巧的體積 (~3-5MB)

## 開發環境要求

- Node.js (>= 18)
- Rust (>= 1.70)
- npm 或其他套件管理器

## 安裝步驟

1. **重要：重新開啟終端機**

   由於我們剛剛安裝了 Rust，需要重新開啟終端機以載入環境變數。

2. **安裝相依套件**

   ```bash
   npm install
   ```

3. **執行開發伺服器**

   ```bash
   npm run tauri:dev
   ```

4. **構建正式版本**

   ```bash
   npm run tauri:build
   ```

## 專案結構

```
markdown-file-reader/
├── src/                  # 前端原始碼
│   ├── main.ts          # 主要進入點
│   └── styles.css       # 樣式檔案
├── src-tauri/           # Tauri 後端 (Rust)
│   ├── src/
│   │   ├── main.rs
│   │   └── lib.rs
│   ├── Cargo.toml
│   └── tauri.conf.json
├── index.html
├── package.json
└── vite.config.ts
```

## 使用說明

1. 啟動應用程式後，點擊「開啟 Markdown 檔案」按鈕
2. 選擇一個 .md 或 .markdown 檔案
3. 檔案內容會自動渲染並顯示
4. 點擊工具列右側的主題切換按鈕（☀️/🌙）來切換明暗模式

## 注意事項

- 應用程式圖示已自動產生並放置於 `src-tauri/icons/` 目錄
- 可以使用 `@tauri-apps/cli` 的 `icon` 命令來產生各種尺寸的圖示

## 技術棧

- **前端**: TypeScript + Vite
- **後端**: Rust + Tauri
- **Markdown 渲染**: marked.js
- **外掛程式**:
  - tauri-plugin-dialog (檔案選擇)
  - tauri-plugin-fs (檔案讀取)
